from pathlib import Path
import bs4

def extract_pdf(pdf_path: Path) -> str:
    pass


# TODO: Needs to be implemented at some later point. For now enough to just manually extract...

def extract_html(html_doc: Path) -> str: # ? str or other format?
    pass